@page
@model Shop.UI.Pages.Admin.IndexModel
@{
}


<div id="app">
    @* anything in this app element, VUE will knows  *@

     <button v-on:click="getProducts">get products</button>

 

    @* above is vue directive, the input bind to the model, v-show is true then generate the DOM otherwise not, it is like angular *@
    
<div>
    <input v-model="productModel.name" />
    <input v-model="productModel.description" />
    <input v-model="productModel.value" />
    <button @@click="createProduct">submit</button>
</div>
    
    
<p v-for="product in products">
    {{product.id}} -{{product.name}}-{{product.description}}-{{product.value}}

</p>
</div>

@section scripts{
    <script src="https://cdn.jsdelivr.net/npm/vue@2.7.8/dist/vue.js"></script>
  
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
  <script src="~/js/admin/main.js"></script>
    @* make sure you js file come after vue.js lib, otherwise system cannot find the object *@
 }

